import styled from "styled-components";

const HeadWrapper = styled.header`
  width: 100%;
  height: 70px;
  background-color: teal;
`;

export default function Header() {
  return <HeadWrapper>헤더임</HeadWrapper>;
}
